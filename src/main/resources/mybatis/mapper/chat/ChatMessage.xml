<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.spark.dating.chat.dao.ChatMessageDao">

  <!-- <select id="testselect"
          resultType="Test">
    SELECT *
	FROM TEST
</select> -->


<insert id="insertChatMessage" parameterType="ChatMessage">
	<selectKey keyProperty="cm_no" resultType="int" order="BEFORE">
	      SELECT SEQ_CHATMESSAGE_NO.NEXTVAL FROM DUAL
	</selectKey>
	INSERT INTO CHATMESSAGE
	VALUES(#{cm_no},#{cm_senduser},#{cm_chatRoomNo},#{cm_msg},#{cm_date})
</insert>

<select id="getChattingMessage" parameterType="int" resultType="ChatMessage">
	SELECT
	    m.M_NAME AS CM_SENDUSERNAME,
	    c.CM_MSG,
	    c.CM_DATE
	FROM CHATMESSAGE c
	JOIN MEMBER m ON c.CM_SENDUSER = m.M_NO
	WHERE c.CM_CHATROOMNO = #{value}
	ORDER BY c.CM_DATE ASC
</select>



</mapper>