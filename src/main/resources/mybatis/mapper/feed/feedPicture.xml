<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.spark.dating.feed.FeedPictureDao">

  <!-- feed insert : 시퀀스로 f_no를 먼저 얻음-->
  <insert id="create"
          parameterType="FeedPicture">
    <selectKey keyProperty="fp_no" resultType="int" order="BEFORE">
      SELECT fp_no_seq.NEXTVAL FROM dual
    </selectKey>
    INSERT INTO feedPicture (fp_no, fp_feedno, fp_attachtype, fp_attachoname, fp_attachdata)
    VALUES (#{fp_no}, #{fp_feedno}, #{fp_attachtype}, #{fp_attachoname}, #{fp_attachdata})
  </insert>
</mapper>