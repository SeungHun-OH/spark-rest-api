<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.spark.dating.feed.FeedDao">

  <!--ResultMap-->
  <resultMap id="feedBase" type="Feed">
    <id property="f_no" column="f_no"/>
    <result property="f_memberno" column="f_memberno"/>
    <result property="f_content" column="f_content"/>
    <result property="f_date" column="f_date"/>
  </resultMap>

  <!--구현-->
  <insert id="create"
          parameterType="Feed">
    <selectKey keyProperty="f_no" resultType="int" order="BEFORE">
      SELECT f_no_seq.NEXTVAL FROM dual
    </selectKey>
    INSERT INTO feed (f_no, f_memberno, f_content, f_date)
    VALUES (#{f_no}, #{f_memberno}, #{f_content}, SYSDATE)
  </insert>

  <select id="selectByFno" parameterType="int" resultMap="feedBase">
    SELECT f_no, f_memberno, f_content, f_date
    FROM feed
    WHERE f_no = #{f_no}
  </select>

  <select id="selectByPage" resultMap="feedBase">
  <![CDATA[
    SELECT rnum, f_no, f_memberno, f_content, f_date
    FROM (
      SELECT rownum AS rnum, f_no, f_memberno, f_content, f_date
      FROM (
        SELECT f_no, f_memberno, f_content, f_date
        FROM feed
        WHERE f_memberno = #{m_no}
        ORDER BY f_date DESC
      )
      WHERE rownum <= #{pager.endRowNo}
    )
    WHERE rnum >= #{pager.startRowNo}
  ]]>
  </select>

  <select id="selectAllByMno" parameterType="int" resultMap="feedBase">
    SELECT f_no, f_memberno, f_content, f_date
    FROM feed
    WHERE f_memberno = #{f_memberno}
  </select>

  <update id="update" parameterType="Feed">
    UPDATE feed
    SET   f_content = #{f_content},
          f_date = SYSDATE
    WHERE f_no = #{f_no}
  </update>

  <delete id="delete"
          parameterType="int">
    DELETE FROM feed
    WHERE f_no = #{f_no}
  </delete>

  <select id="countAll" parameterType="int" resultType="int">
    SELECT count(*)
    FROM feed
    WHERE f_memberno = #{f_memberno}
  </select>

</mapper>