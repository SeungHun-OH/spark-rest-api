<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.spark.dating.member.MemberPictureDao">

  <resultMap id = "MemberPicture" type = "MemberPicture">
    <id property="mpNo" column="MP_NO"/>
    <result property="mpMemberNo" column="MP_MEMBERNO"/>
    <result property="mpAttachType" column="MP_ATTACHTYPE"/>
    <result property="mpAttachOname" column="MP_ATTACHONAME"/>
    <result property="mpAttachData" column="MP_ATTACHDATA"/>

  </resultMap>

  <insert id = "insertMemberPicture" parameterType="memberPicture">
    insert into memberpicture (
     MP_MEMBERNO, MP_ATTACHTYPE, MP_ATTACHONAME, MP_ATTACHDATA
    ) values (
     #{mpMemberNo}, #{mpAttachType}, #{mpAttachOname}, #{mpAttachData,jdbcType=BLOB}
    )
  </insert>

  <select id="SelectMemberPictureByM_no" parameterType="int" resultMap="MemberPicture">
  SELECT *
  FROM memberpicture
  WHERE MP_MEMBERNO = #{mNo}
  </select>

</mapper>