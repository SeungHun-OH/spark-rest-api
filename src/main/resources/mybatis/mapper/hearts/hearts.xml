<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.spark.dating.hearts.HeartsDao">

  <!--구현-->
  <insert id="create"
          parameterType="Hearts">
    <selectKey keyProperty="hNo" resultType="int" order="BEFORE">
      SELECT h_no_seq.NEXTVAL FROM dual
    </selectKey>
    INSERT INTO hearts (h_no, h_senduser, h_receiveuser, h_date, h_requestchanel)
    VALUES (#{hNo}, #{hSendUser}, #{hReceiveUser}, SYSDATE, #{hRequestChanel})
  </insert>

  <select id="selectByHno" parameterType="int" resultType="Hearts">
    SELECT h_no, h_senduser, h_receiveuser, h_date, h_requestchanel
    FROM hearts
    WHERE h_no = #{hNo}
  </select>

  <select id="selectByChanel" resultType="Hearts">
    SELECT h_no, h_senduser, h_receiveuser, h_date, h_requestchanel
    FROM hearts
    WHERE h_receiveuser = #{m_no}
      AND h_requestchanel = #{h_requestchanel}
  </select>

  <delete id="delete"
          parameterType="int">
    DELETE FROM hearts
    WHERE h_no = #{hNo}
  </delete>

</mapper>